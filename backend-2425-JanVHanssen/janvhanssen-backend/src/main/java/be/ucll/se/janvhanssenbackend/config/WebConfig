package be.ucll.se.janvhanssenbackend.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("https://frontend-2425-janvhanssen.vercel.app")
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
                // Voeg 'Accept-Encoding' expliciet toe aan de toegestane headers
                .allowedHeaders("Authorization", "Content-Type", "Accept", "Accept-Encoding") // <-- AANGEPAST
                .allowCredentials(true);
    }
}